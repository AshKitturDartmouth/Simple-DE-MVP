# Simple-DE-MVP
Working MVP, meant to be simple. Takes in live data, runs through ETL pipelines within a medallion architecture, data is sent to DuckDB with dbt connected, Airflow can configure the pipelines and there should be some way to view/configure the spark clusters within the pipelines.
